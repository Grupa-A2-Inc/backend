spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/adaptive_tutor_test
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: true
  # Dezactivează importul din .env — nu există pe CI
  config:
    import: ""

# Dezactivează mail, AI etc. pentru teste
app:
  mail:
    from: test@test.com

ai:
  api:
    base-url: http://localhost:9999
    key: test-key
    timeout-generate-ms: 10000
    timeout-feedback-ms: 5000
    timeout-adaptive-ms: 10000
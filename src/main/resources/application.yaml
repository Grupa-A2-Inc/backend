spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/adaptive_tutor
    username: user
    password: password
  flyway:
    enabled: true

server:
  port: 8067